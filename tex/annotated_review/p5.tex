\section{Computational results} \label{sec:results}
The experiments reported in this section have been solved using Gurobi 12.0.2 on a computer equipped with an Intel(R) Xeon(R) Gold 6244 CPU @ 3.60GHz processor and 384 GB of RAM.

\begin{atodo}
Add threads per solve, whether runs were executed concurrently, and the Gurobi
licence class. This matters more than usual here, because
Section~\ref{sec:la-config} reports \emph{per-stop} decision time rather than
wall clock precisely on the grounds that wall clock across configurations would
measure contention. If that is true there, it is true in
Table~\ref{tab:runtime} too.
\end{atodo}

\subsection{Validation of the deterministic model on TDSP benchmark instances}
\label{sec:validation}

Before turning to the stochastic experiments, we verify that the HoS core of the deterministic MILP of Section~\ref{sec:model} reproduces known optimal solutions of the pure truck driver scheduling problem. To this end we use the 40 R15-PGLT benchmark instances made available by \citet{garaix_label-setting_2024}\footnote{\url{https://emse.fr/~garaix/TruckDriverSchedule/Expert_Systems/}}, which comprise fixed customer sequences with 2--14 customers, per-stop service times (including a loading operation at the origin depot), hard time windows, and all durations rounded to multiples of 15~minutes. We compare against the published optima of their \emph{no-night} configuration, which enforces exactly the daily provisions of Regulation~(EC) No~561/2006 and Directive 2002/15/EC considered in this paper, without the night-work rule.

Since these are diesel instances, the benchmark exercises the HoS machinery of our model in isolation: we disable the battery ($E_i = 0$,$\mathcal K = \emptyset$) and reduce all maneuver, queue, and out-of-window penalty terms to zero, with time windows enforced as hard constraints ($\delta_i = 0$). Furthermore, the reference model allows breaks and rests to preempt driving and service at arbitrary points. We emulate this on the 15-minute by inserting rest-area nodes ($\mathcal L$) every 15~minutes of driving and by splitting each service operation into 15-minute segment\FIX{s}.

\begin{afix}
\textbf{Restore the four bridging differences you commented out.} This is the
paper's only external validation, and its credibility is exactly the credibility
of this list --- a referee who cannot see how the two formulations were reconciled
cannot take 39/40 at face value. Suggested, condensed to keep the length you
have now:

\medskip
\FIX{Four further differences are bridged as follows. A zero-length rest-area node
before each customer permits the arrive--rest--serve pattern present in several
reference optima. Their schedules must terminate with a daily rest at the
destination, whose duration counts toward the objective, so we add
$\rho^{r_1}_{N} + \rho^{r_2}_{N} = 1$. The departure time from the origin is left
free, bounded below by the earliest start. Finally the working-time provisions of
Directive 2002/15/EC --- at most 6\,h of cumulated work without a 15-minute break,
and at least 30 (resp.\ 45) minutes of cumulated breaks in a shift whose working
time exceeds 6 (resp.\ 9) hours --- which our base model verifies ex post, are
enforced in-model here by two additional accumulators mirroring
\eqref{eq:cd_prop}--\eqref{eq:cd_ub}. An inspection of the 40 reference schedules
confirms that all their optima lie on the 15-minute grid, so the discretization
is lossless on this set.}
\end{afix}

Table~\ref{tab:validation} summarizes the outcome. Our formulation, solved with Gurobi to zero optimality gap, reproduces the published optimal objective exactly on 39 of the 40 instances, with solve times below 64~s (median below 1~s) against instance sizes of up to 180 stops after discretization. On the single divergent instance (Test~4) our model attains 1410~min against a published value of 1425~min\FIX{; a manual verification of our schedule against the constraints of the reference formulation confirms that it satisfies all of them, so the published value appears to be marginally suboptimal on that instance. We conclude that the split-break logic, the reduced-rest and extended-driving budgets, and the shift-spread construction of Remark~1 are consistent with an independently published exact method.}

\begin{table}[htbp]
\centering
\caption{Validation on the 40 R15-PGLT instances (no-night configuration).}
\label{tab:validation}
\small
\begin{tabular}{lr}
\toprule
Instances solved to proven optimality & 40/40 \\
Exact matches \FIX{with published optima} & 39/40 \\
Divergent instances & 1 (ours 1410 vs.\ 1425, see text) \\
Stops after 15-min discretization & 23--180 \\
Solve time (median / max) & $<1$~s / 64~s \\
\bottomrule
\end{tabular}
\end{table}

\begin{acmt}
\textbf{This subsection is undersold.} Reproducing 39 of 40 published optima of an
independent exact label-setting algorithm, and demonstrably improving on the
40th, is an unusually strong validation for this literature, where models are
typically checked only against themselves. It currently reads as a warm-up.
Consider reporting the per-instance comparison in an appendix table, and stating
plainly that Test~4's published value is suboptimal --- that is a small result of
its own.
\end{acmt}

\subsection{Base-case comparison}
\label{sec:basecase}

Each instance class is instantiated with 50 independent random realizations, and every realization is solved by all five methods (Greedy, RO, ROBU, LA, SP) and evaluated in the common discrete-event simulator of Section~\ref{sec:simulation} against the perfect-hindsight oracle.
Every offline optimisation model (RO, \FIX{ROBU,} the two-stage stochastic program, and the oracle) was solved with Gurobi under a gap tolerance of 0.5\% and a time limit of 7,200s. LA's look-ahead subproblems are LP relaxations with a 300s cap that is never binding in practice. Greedy uses no solver.

We compare the methods along three metrics. First, efficiency, shown through the gap to oracle in realized route duration in Table~\ref{tab:gap} and Figure~\ref{fig:resultsgap}. Then, reliability, via the rate at which the executed schedule becomes infeasible (the infeasibility strip of Figure~\ref{fig:resultsgap} and Table~\ref{tab:feasibility}). Finally, computational effort in Table~\ref{tab:runtime}.

Because the oracle is itself a mixed-integer program, the reference against which every method gap is measured is only as good as the oracle gap. On short and medium routes the oracle closes to optimality for most instancces. However, on half of long routes the incumbent stabilizes early while the dual bound stalls at roughly \prov{9\%}. Long-route gaps in Table~\ref{tab:gap} are therefore not exact distances to the proved optimum, but only to the best incumbent.

\input{gap_marked}

\begin{afix}
\prio{The caption is not true as written.} It says ``averaged over 50
realizations per instance class'', but infeasible runs are excluded from the gap
(as Section~\ref{sec:simulation} states), so the real denominators run from 50
down to \provlow{4}. Suggested caption change: replace ``averaged over 50
realizations per instance class'' with \FIX{``averaged over the realizations that
were both feasible and matched to an oracle bound; cells highlighted in yellow
rest on fewer than 45 such realizations, and cells in red on fewer than
twenty''}.

\medskip
The worst offenders, from \texttt{paper\_gap\_stats.csv}:
ROBU \emph{Medium/Few/Large} \provlow{$n = 1$} --- one instance, currently setting
the top of the reported ROBU range; 2SP \emph{Long/Medium/Tight}
\provlow{$n = 4$}; 2SP \emph{Long/Many/Tight} \provlow{$n = 6$}; 2SP
\emph{Long/Few/Tight} \provlow{$n = 9$}. Twelve cells sit below $n = 20$ and all
twelve are 2SP on long routes.

\medskip
Recommendation: suppress every cell with $n < 20$, and delete the ROBU medium row
outright. \TODO{re-check once 2SP finishes --- the long-route cells should fill
in, and the yellow should mostly clear.}
\end{afix}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/resultsgap.pdf}
    \caption{Distribution of the realized gap to the oracle across seeds, by route class, customer count, method (colour) and time-window class (shade, Tight--Medium--Large--None).  Boxes span the interquartile range, whiskers 1.5\,IQR.  The heat strip below each panel shades the infeasibility rate. Empty slots are instance/method combinations without completed runs.}
    \label{fig:resultsgap}
\end{figure}

\begin{atodo}
Two fixes to the figure itself, neither structural. (i) The infeasibility strip
uses a green$\to$\emph{orange} ramp that collides with RO's vermillion and
ROBU's orange in the method palette; a single-hue sequential ramp that no method
owns would remove the ambiguity. (ii) Empty slots are silent --- annotate them
``no runs'' so absence is not read as zero.
\end{atodo}

Across short and medium instance classes the two-stage stochastic plan (SP) and the look-ahead policy (LA) attain the smallest gaps to the oracle, as seen in Figure \ref{fig:resultsgap} and Table \ref{tab:gap}. The myopic greedy heuristic is intermediate, while both static robust plans are the most conservative.
This ordering follows the amount of information each method uses and the point at which it commits: the two policies that adapt to realized travel times (SP through online duration recourse and LA through full re-optimisation at every stop) recover most of the hindsight optimum, whereas the offline robust plans pay for committing every duration in advance against a possible worst case.
The ordering between SP and LA reverses with route length: on the short routes SP is clearly ahead (\DEL{5.5\% vs.\ 12.3\%}\prov{4.9\% vs.\ 11.6\%} on average) because 25 scenarios over the whole horizon still capture the realization well, while on the medium routes the two are indistinguishable (\DEL{13.8\% each}\prov{12.9\% and 13.0\%}) and on the long routes SP terminates due to time limit because of its exponential size, meaning large optimality gap and therefore potential worse solutions given to the simulation framework\FIX{; over the long-route cells where it produces a schedule at all its mean gap is \prov{28.3\%}, and it strands on so many realizations that several of those cells rest on fewer than twenty surviving runs}. Greedy, conversely, improves in relative terms as routes lengthen (\DEL{21.3\%, 19.2\%, 14.4\%}\prov{21.1\%, 19.2\%, 14.1\%} mean gap on short, medium and long routes).

\begin{acmt}
\prov{Yellow} throughout this paragraph: these are the aggregates most sensitive
to the 2SP runs still finishing. The \emph{ordering} is stable --- LA
\prov{11.9\%} $<$ 2SP \prov{15.4\%} $<$ Greedy \prov{18.1\%} $<$ ROBU
\prov{25.3\%} $<$ RO \prov{34.6\%} overall --- and the reversal with route length
is stable too, so the conclusion does not depend on the pending runs, as you
say. Only the digits do.
\end{acmt}

The gap widens sharply with the number of customers and the tightness of the windows. On the short routes the \emph{Few}/\emph{Large} cell against the \emph{Many}/\emph{Tight} show a two to three time deterioration for the three committing methods and less than a doubling for the two adaptive ones. A static schedule that must satisfy more, and narrower, service windows simultaneously is exactly the situation in which committing early is most expensive.

A substantial part of the gap on the window-constrained classes is window penalty rather than slower routing. Comparing the penalized gap of Table~\ref{tab:gap} with the duration-only gap, the $\beta\sum_i\delta_i$ term accounts on average for \prov{16\%} of RO's gap and \prov{21\%} of Greedy's, and on the tight-window classes for \prov{25\%} of RO's against \prov{38\%} of Greedy's. The penalty share is however smallest for the box robust plan: RO is genuinely slower, spending its gap on conservative durations, while Greedy's tight-window gap is mostly the cost of arriving off the windows: up to two-thirds of it on \FIX{\emph{Short}/\emph{Many}/\emph{Tight}} (\prov{41.6\%} penalised vs.\ \prov{14.0\%} duration-only). The adaptive policies sit in between (LA \prov{38\%}, SP \prov{35\%} on tight windows), consistent with policies that buy window compliance with a little extra time. \FIX{On the window-free classes the two definitions coincide exactly.}

\begin{acmt}
The sentence had a dangling ``up to two-thirds of it on (42.8\% penalised
vs.\ 14.5\% duration-only)'' with the cell name missing --- it is
\emph{Short}/\emph{Many}/\emph{Tight}, now \prov{41.6} vs \prov{14.0}.
\end{acmt}

Where both static robust variants terminate (the twelve short-route classes) the budgeted plan (ROBU) improves on the box plan (RO) in every single cell, by \DEL{4.8 to 15.0}\prov{4.7 to 14.8} percentage points. This confirms that pricing only a bounded number of legs at their worst case removes a large part of the box's conservatism while retaining a guarantee (Section~\ref{sec:robu}).

\begin{atodo}
``Improves in every single cell'' over twelve paired cells is a sign test,
significant at $p < 0.001$ --- say so. More generally the paper contains
\textbf{no statistical testing at all}, which with 50 paired seeds per cell is
cheap to fix: a Wilcoxon signed-rank test or a paired bootstrap CI on SP$-$LA per
route class would let you write ``indistinguishable'' with a number attached
rather than as an impression, and the same machinery covers the diesel penalty of
Section~\ref{sec:diesel}.
\end{atodo}

\DEL{XXX something about no-split instances. used in XXX \% of oracles runs. Does not provide substantial benefit, no split instance have similar results.}

\begin{anew}
The no-split family exists (\texttt{instances\_sens/no\_split\_*}) and the
finding is worth two sentences, because it is regulation-facing and negative:

\medskip
``Finally, we test whether the split-break provision of Article 7 is worth its
modelling complexity. Re-solving the reduced set with $x^{b_{15}}$ and
$x^{b_{30}}$ removed, so that every break must be an unsplit 45 minutes, changes
realized route duration by \prov{less than 0.5\,\%}, below the solver tolerance
on the oracle, even though the hindsight optimum uses a split at \prov{38\,\%} of
instances where it is available. The option is exercised often but is almost
never binding: on an electric truck the charging stop already supplies a break
long enough that the flexibility to divide it buys nothing.''

\medskip
\TODO{\textbf{verify before publishing this.} 156 oracle caches in this family
were found to contain git conflict markers, so the current numbers cannot be
trusted as they stand. Re-run, then fill the two yellow figures.}
\end{anew}

\input{feasibility}

\begin{afix}
\textbf{Every prose number in the next three paragraphs disagreed with this
table} and has been rewritten against the 2026-08-07 regeneration. The drift,
for the record:

\medskip
\begin{center}\footnotesize
\begin{tabular}{lcc}
\toprule
 & manuscript & \textbf{current table} \\
\midrule
Greedy HoS violation  & 1.8\,\%  & \textbf{2.3\,\%} \\
Greedy stranding      & 0.2\,\%  & \textbf{0.0\,\%} \\
Greedy TW hit         & 55.1\,\% & \textbf{55.5\,\%} \\
SP stranding          & 11.6\,\% & \textbf{15.5\,\%} \\
SP TW hit             & 86.6\,\% & \textbf{77.9\,\%} \\
SP not optimal        & 38\,\%   & \textbf{48.5\,\%} \\
SP final MIP gap      & 12.5\,\% & \textbf{21.1\,\%} \\
RO not optimal        & 33.1\,\% & \textbf{38.1\,\%} \\
RO TW hit             & 41.2\,\% & \textbf{40.8\,\%} \\
LA HoS / stranding    & 0.5 / 1.0 & \textbf{1.2 / 2.0} \\
LA TW hit             & 69.7\,\% & \textbf{68.9\,\%} \\
\bottomrule
\end{tabular}
\end{center}

\medskip
\textbf{The permanent fix is a macro file.} Have
\texttt{compile\_solutions.py} emit \texttt{macros.tex} of
\verb|\newcommand|s (\verb|\SPstrand|, \verb|\GreedyHoS|, \dots) and cite the
macros in the prose. This class of error will otherwise recur at every
regeneration, and it is the one a referee with the repository will find first.
\end{afix}

Efficiency alone is misleading, because a low gap can hide a high failure rate: infeasible runs (a stranding or an Hours-of-Service breach in execution) carry no meaningful gap and are excluded from the averages, so a method that fails often is scored only on the realizations that survives. The infeasibility strip of Figure~\ref{fig:resultsgap} and Table~\ref{tab:feasibility} restore this axis.

The two-stage plan attains the best window compliance in the study (TW hit \DEL{86.6\%}\prov{77.9\%}) and among the lowest gaps, but only through near-permanent online recourse: it repairs its committed structure on \prov{99.2\%} of runs and still strands the vehicle on \DEL{11.6\%}\prov{15.5\%} (by an order of magnitude the highest stranding rate of the five methods). Furthermore, \DEL{38\%}\prov{48.5\%} of SP solves returned at the time limit without a proven optimum, \FIX{at a mean final gap of \prov{21.1\%},} therefore providing potential low quality solution to te simulation.
The robust plans never breach the Hours-of-Service rules and never strands either, by construction, at the cost of the worst window compliance (\DEL{41.2\%}\prov{40.8\%}) and the largest gaps. Its \DEL{33.1\%}\prov{38.1\%} not optimal share is driven by the long-route instances, where the offline solve reaches the time limit.
ROBU trades a 3.3\% stranding rate for a \DEL{22}\FIX{26}-point gain in window hit rate (67.3\%) and a \FIX{nine}-point gain in gap. However solving it in larger than short instances become intractable. \FIX{A further 6.1\,\% of ROBU runs were never certified within the wall-clock budget and are excluded from the rates above; no other method leaves any run uncertified.}
The greedy heuristic \DEL{rarely strands (0.2\%)}\FIX{never strands (0.0\%)} but breaches driving limits on \DEL{1.8\%}\prov{2.3\%} of runs, and misses the most windows after RO (TW hit \prov{55.5\%}), consistent with a policy that stops only when forced.
The look-ahead policy is the only method that is simultaneously highly feasible (\DEL{0.5\% HoS, 1.0\% stranding}\prov{1.2\% HoS, 2.0\% stranding}) and window-compliant (\DEL{69.7\%}\prov{68.9\%}), which is what makes it the most defensible operational recommendation even though SP dominates it on the short routes.

\begin{acmt}
Greedy's stranding rate of \emph{exactly} zero deserves a clause, because it looks
surprising for a myopic rule and it is not luck: it follows from decision rule (3),
which charges whenever the energy to the next station exceeds usable SOC, and
from charging to full whenever it stops. Greedy is myopically energy-safe and
myopically time-reckless, and its \prov{2.3\,\%} HoS breach rate is the mirror
image. That contrast is the cleanest illustration in the paper of the two-tail
structure of the uncertainty (Section~\ref{sec:uncertaintylit}).
\end{acmt}

\begin{atodo}
\textbf{Survivorship is acknowledged but never priced.} Runs excluded from the
gap averages, over the whole grid: 2SP \prov{225}, LA \prov{76}, Greedy
\prov{35}, ROBU 20 plus 39 never certified, RO 0. So 2SP's headline short-route
gap is computed on the realizations it survived and RO's on all of them. One
extra scalar closes this: expected realized duration with infeasible runs charged
at a stated recovery cost (a forced 11\,h rest for a HoS breach, a roadside
recovery for a stranding). The paper's operational recommendation rests on
exactly this trade-off and currently asks the reader to compute it across two
tables.
\end{atodo}

\input{runtime}

\begin{afix}
Column headers read Small / Medium / Large and must be Short / Medium / Long.
\textbf{SP now has a long-route row} (\prov{7\,207\,s} offline, i.e.\ at the
limit, \prov{0.58\,s} per stop), so the claim below that it does not run at all
on long routes is no longer true and has been rewritten. The Oracle row is
present but empty because solve time is not stored in the oracle cache ---
\TODO{either store it or drop the row}.
\end{afix}

Finally, the methods differ by orders of magnitude in cost, and along two distinct dimensions: an offline solve committed before departure and an online decision cost paid at every stop (Table~\ref{tab:runtime}). Greedy is effectively instantaneous ($<10^{-4}$\,s per stop). The offline plans pay their cost upfront and then execute for free: RO in 63\,s on the short routes, 2\,313\,s on the medium and \prov{7\,169\,s} on the long ones. SP in \prov{1\,595\,s} and \prov{6\,596\,s}\DEL{ and not at all on the long routes}\FIX{, and on the long routes only by exhausting the 7\,200\,s budget}; and ROBU the most expensive at 5\,024\,s and \prov{14\,347\,s}, owing to its iterative column-and-constraint generation (Section~\ref{sec:robu}).  ROBU's medium-route figure is the average of the runs that finished, which is why the class is essentially absent from Table~\ref{tab:gap}: the method is limited by its offline budget, not by solution quality.
LA carries the opposite profile: no offline solve, but \DEL{14--23}\prov{14--22}\,s of scenario optimization \emph{per stop}, growing only mildly with route length. That is comfortably real-time against the 60\,km charger spacing of the base case (a decision every $\approx$40\,min of driving), but it accumulates: on a long route with $\approx$\FIX{170} stops LA spends more wall-clock time in total than RO spends offline.

\begin{acmt}
``$\approx$100 stops'' on a long route was an underestimate: the mean is
\FIX{167} (Section~\ref{sec:instances}), which makes the point stronger, not
weaker --- roughly one hour of cumulative on-board compute per long route.
\end{acmt}

\subsection{Additional analyses}
\label{sec:additional-protocol}

The base-case instances of Section~\ref{sec:basecase} are too expensive to re-run for every sensitivity, so the analyses of Sections~\ref{sec:sensitivity}--\DEL{8.3.4}\FIX{\ref{sec:diesel}} use a reduced set. All additional experiments run on the short and medium route classes with few and many customers, all four window classes, and seeds 1--10: $2 \times 2 \times 4 \times 10 = 160$ instances. The long routes are excluded. Oracle route durations on this subset average \DEL{27.4\,h (short) and 54.6\,h (medium)}\prov{28.7\,h (short) and 58.2\,h (medium)}.
The additional analysis use Greedy, LA and the oracle while SP, RO and ROBU are omitted.

\begin{afix}
\verb|\ref{sec:uncertainty}| pointed at a label that does not exist, so the
compiled paper reads ``Sections 8.3.2--??''. Retargeted to
Section~\ref{sec:diesel}, the last of the additional analyses.
\end{afix}

\begin{anew}
Two protocol sentences are missing and they are what make these numbers strong.
Suggested addition after the first sentence:

\medskip
``Each variant is materialized as a separate instance file, so its runs and its
oracle cache never collide with the base ones. Variants that change the corridor
--- charging-station spacing and charger power --- are regenerated from the same
(route, customers, window, seed) key, so geometry and customer draws stay paired
with the base instance; the diesel variant is applied to a verbatim copy, so
geometry \emph{and} travel-time realization are identical. Every reported effect
is a mean over paired instances of the relative change in route duration with
respect to the base case, restricted to pairs in which both runs were feasible.''

\medskip
Without this a reader cannot tell whether the sensitivity numbers are paired
differences or differences of class means --- and they are the former, which is a
much stronger design.
\end{anew}

\subsubsection{Look-ahead sensitivity} \label{sec:la-config}
The rolling-horizon look-ahead method is parameterized through several options. First, the look-ahead horizon, which determines how far ahead we look. While the base case considers a horizon of 24 hours, additional testing is done with both shorter and longer horizons. Second, the number of scenarios considered when selecting an action provides information about the distribution of uncertainty and greater insight into potential extreme scenarios. Finally, to achieve a reasonable solve time compatible with real-life use, the base case considers only the LP relaxation of the subproblem, whereas here we examine solving the full MILP subproblems.

\begin{anew}
Worth one added sentence on \emph{why} these horizons: ``The horizon values are
not a smooth grid, because the axis is not expected to give a smooth response.
Under the rules of Section~\ref{sec:model} a driver's day is bounded by a 13\,h
spread that must close with an 11\,h daily rest, so 24\,h is exactly one duty
cycle: a 12\,h horizon ends before the current duty does and can never see the
rest that terminates it, 24\,h sees one complete cycle, and 48\,h sees two.''
\end{anew}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/resultsLA.pdf}
    \caption{\DEL{Enter Caption}\NEW{Look-ahead configuration sweep, one axis per column: horizon at the base scenario count (left) and scenario count at the base horizon (right). Top row, median gap to the hindsight optimum; bottom row, median per-stop decision time. Circled markers are the base cell, which lies on both ladders. Solid lines are the window-free class, dashed the tight-window class. The strip below each column shades the infeasibility rate per route class.}}
    \label{fig:resultsLA}
\end{figure}

\DEL{We observe in Figure \ref{fig:resultsLA} that XXXX}

\input{additional_la}

\begin{anew}
\textbf{The sweep is complete except MIPTAIL, so the paragraph can be written
now.} Suggested:

\medskip
``\emph{The horizon axis behaves as a threshold, at exactly the duty cycle.}
Shortening the window from 24\,h to 12\,h costs $+0.3\%$, $+0.9\%$ and $+1.5\%$ of
realized duration on the short, medium and long routes under no windows, and
$+1.7\%$, $+0.5\%$ and $+0.9\%$ under tight ones. On the window-free class the
penalty is monotone in route length, as the truncation argument predicts: the
longer the route, the more often a 12\,h horizon ends mid-duty and the policy must
place a rest whose end it cannot see. Lengthening the window to 48\,h, by
contrast, changes realized duration by between $-0.2\%$ and $+0.2\%$ everywhere,
indistinguishable from zero, while the median decision time rises from 22\,s to
52\,s on the long routes. Seeing beyond the current duty cycle buys nothing;
seeing less than one costs about a percent.

\medskip
\emph{The scenario axis is flat over the range tested.} Moving from 25 scenarios
to 10 changes realized duration by between $-0.2\%$ and $+0.4\%$, and moving to 50
by between $-0.2\%$ and $+0.3\%$, in every route and window class, against a
decision-time ratio of roughly $4.5\times$ across that range (9\,s to 43\,s on the
long routes). The effect is not concentrated under tight windows, contrary to
what the coupling argument would suggest. The plausible reading is that the
scenario set is used to \emph{rank} a small discrete action set rather than to
estimate a quantity, and ten draws already separate the actions that matter.

\medskip
Taken together, the recommended on-board configuration is $L = 24$\,h with
$|\Xi| = 10$, which halves the per-decision cost from 22\,s to 9\,s on the long
routes at no measurable loss in schedule quality, and the horizon should not be
cut below one duty cycle at any scenario count.''
\end{anew}

\begin{atodo}
Two checks before publishing the paragraph above. (i) \textbf{Dispersion, not just
medians}: if ten scenarios matched twenty-five in the median but had a heavier
right tail, the conclusion reverses. Add an IQR column or a box plot of the paired
deltas. (ii) \textbf{Infeasible count per cell}: a configuration that is faster
because it strands the truck is not faster. Report $n_{\text{paired}}$ per cell.
\end{atodo}

\input{additional_la_policy}

\begin{anew}
The policy variants belong in this subsection too, since the manuscript already
promises the MIP-tail comparison. Suggested, with the pending cells flagged:

\medskip
``Two further variants isolate policy choices rather than sizing. TB0 removes the
five-minute tie-break that buys opportunistic charging; it costs between
$-0.08\%$ and $+0.29\%$ of realized duration at unchanged decision time, so the
tie-break is essentially free but also nearly worthless, and the policy is
insensitive to it. MIPTAIL solves the look-ahead tail as a MIP rather than an LP
relaxation. On the one cell available so far --- long routes, no windows --- it
reduces the gap to the hindsight optimum from \prov{6.7\%} to \prov{4.7\%}, a
paired improvement of \prov{$-2.58\%$} in realized duration, but at
\prov{487\,s} per stop against 22\,s, a factor of \prov{22}. The LP relaxation is
therefore not free: it costs roughly \prov{two points} of optimality. It remains
the right default, because 487\,s per stop is not deployable in a cab, but the
gap it leaves is now quantified rather than assumed negligible.''

\medskip
\TODO{\textbf{MIPTAIL is the only outstanding LA run.} Five of the six cells are
empty. Everything highlighted above rests on a single cell and must be re-read
when the short, medium and tight-window cells land --- in particular whether the
two-point improvement holds where the LP relaxation is under less strain.}
\end{anew}

\subsubsection{Sensitivity analysis on charging infrastructure}
\label{sec:sensitivity}

The base case fixes a 500\,kWh battery charged on a three-segment concave piecewise-linear curve reaching full charge in 2.5\,h, an average of $\approx$200\,kW, and charging stations every 60\,km as mandated for the TEN-T core network by Regulation~(EU) 2023/1804.  We perturb these two infrastructure parameters one at a time.
The charger-power axis rescales the time breakpoints of the charging curve inversely, $\bar{T} \mapsto \bar{T}\,P_{\mathrm{base}}/P$, and therefore changes how strongly the concave tail of the curve bites.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/add_sens.pdf}
    \caption{One-at-a-time sensitivity of mean route duration to the charging
    infrastructure \DEL{and uncertainty parameters}, relative to the base case, over
    the 160 paired instances}
    \label{fig:sens}
\end{figure}

\begin{acmt}
No uncertainty axis appears in this figure --- the CV and $\beta$ sweeps were
removed from scope --- so the caption promises a panel that is not there.
\end{acmt}

\input{additional_sensitivity}

\begin{acmt}
The generated table is included above because it carries the LA column, which
the text does not currently mention and which is a useful third data point:
LA sits between Greedy and the oracle on every row, so the over-reaction to
charger power is a monotone function of planning quality rather than a
Greedy-specific artefact.
\end{acmt}

Halving the CS spacing from 60 to 30\,km buys only 1.2\% of route duration at the hindsight optimum, while doubling it to 90\,km costs 1.5\%: at the mandated spacing the corridor is already dense enough that the binding constraint is how long a charging stop takes, not how far away the next one is.
Upgrading the charger from the 200\,kW base to 350\,kW saves \DEL{4.2\%}\FIX{4.3\%}, and going all the way to a 1\,MW megawatt-charging system saves 5.3\%, so a 350\,kW charger already captures four fifths of the benefit available at five times the power. Downgrading to 150\,kW costs \DEL{4.3\%}\FIX{4.2\%}.
\FIX{The asymmetry is a direct consequence of the concave charging curve: at low power the truck spends its time in the slow final segment and adding power shortens exactly that segment, whereas once the fast segments are short relative to the mandatory break and rest periods, extra power has nothing left to remove.}
Investment should therefore go into more power per charger in each CS rather than into a denser than necessary charging infrastructure.

\begin{afix}
The two figures were swapped: from Table~\ref{tab:sensitivity} the 350\,kW
upgrade saves $4.27\%$ and the 150\,kW downgrade costs $4.20\%$.
\end{afix}

Two secondary observations are worth noting.
First, the effect is larger on the medium routes than on the short ones on every power level\FIX{ ($-5.0$ vs.\ $-3.6$ at 350\,kW, $-6.1$ vs.\ $-4.5$ at 1\,MW)}, because a longer route accumulates more charging events over which the saving adds up, whereas the spacing effect is almost route-length independent\FIX{ ($+1.44$ vs.\ $+1.51$ at 90\,km)}.
Second, Greedy reacts more strongly to the charger power changes by about a factor of two relative to the oracle ($-9.61$ vs.\ $-4.\FIX{27}$ at 350\,kW). This is a property of the policy: Greedy charges to full whenever it stops, so it sits on the slow tail of the charging curve far more often than an optimal plan does, and it therefore benefits disproportionately from a faster charger. \FIX{An operator running without planning tools would therefore over-estimate the value of a charger upgrade by roughly a factor of two, and symmetrically over-estimate the damage of a slower one; the look-ahead policy sits between the two ($-4.54$ at 350\,kW).}

\subsubsection{Comparison with diesel trucks}
\label{sec:diesel}

Every instance of the subset in Section~\ref{sec:additional-protocol} is re-solved as a diesel vehicle. Diesel mode removes the energy dimension entirely while keeping the node set, the customer windows and the full Hours-of-Service machinery intact. Former charging stations remain as rest areas, so the diesel problem is a genuine relaxation of the electric one on the same corridor and the same travel-time realization, and the two are solved by the same oracle and the same greedy policy.

\begin{anew}
Two accounting decisions are made here and neither is stated. Both are
contestable, and stating them is what makes the result defensible rather than
merely favourable:

\medskip
``Of the station overheads, only the charger-specific ones are removed: the queue
and the repositioning move off the charging bay. The access manoeuvre is
\emph{not}, because it is not charger-specific --- a diesel driver taking a
mandatory break also has to leave the motorway, park and re-enter, and pays the
same $M^{stop}$. Charging it to only one of the two vehicles would credit the
diesel with taking every break at no access cost, which overstates the
electrification penalty by roughly two percentage points.

\medskip
Refuelling is credited post hoc rather than modelled: unlike charging it has no
binding location choice, and on these corridors it happens at most twice, since
route lengths run 824--1\,219\,km (short), 1\,541--2\,536\,km (medium) and
3\,042--4\,025\,km (long) against a usable tank range of 1\,636--3\,240\,km. We
credit the diesel with zero fuel stops on the short routes, one on the medium and
two on the long, at 19~minutes each. Crediting the stop lengthens the diesel
makespan and therefore \emph{shrinks} the reported penalty, so the zero-stop
figures reported alongside are the conservative bound.''
\end{anew}

\input{additional_diesel}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\linewidth]{figures/diesel.pdf}
    \caption{Electrification penalty in route duration, per route class: \DEL{the naive estimate (diesel makespan plus the EV's total charging time) against} the penalty actually realized by the greedy policy and by the hindsight optimum.  The annotation below each group gives the mean coupling share $\sum_i g_i/\sum_i \tau^c_i$ and the number of instances.}
    \label{fig:diesel}
\end{figure}

\begin{afix}
The caption describes three bars including a ``naive estimate''; the figure has
two. \TODO{the naive-estimate bar is actually worth adding back --- it is the
quantity a practitioner would compute, and showing that the realized optimum
beats it by the coupling credit is the clearest possible statement of what
break/charge synchronization is worth.}
\end{afix}

\begin{anew}
\textbf{The section stops after the setup: there are no results.} Diesel is
complete per your status, so this can be written in final form now.

\medskip
``Electrification carries a real and irreducible makespan penalty on these
corridors: $+8.0\%$ on the short routes, $+9.4\%$ on the medium and $+10.8\%$ on
the long ones at the hindsight optimum, positive on every one of the 160
instances. Break/charge coupling is effective in the sense the model predicts,
since 68--76\% of charging time is credited inside a mandatory break, but it is
far from cancelling the cost of charging. Table~\ref{tab:diesel-decomp}
decomposes the penalty into the terms of the departure equations; driving is
identical within each pair, so the modelled rows close the gap exactly.

\medskip
The coupling credit --- the break time the driver no longer has to take
separately --- is 1.25\,h, 3.19\,h and 4.93\,h, but it is bounded above by the
break duration the regulation requires in the first place: charging for longer
than 45~minutes at a stop earns no further credit. Against it the electric truck
pays 0.71\,h, 1.47\,h and 2.71\,h of station access that the diesel does not: the
queue, the bay repositioning, and the residual manoeuvring of the stops it makes
\emph{only} to charge. Note how small that last term is once the diesel is
charged for its own break access --- the manoeuvring row is 0.37\,h, 0.63\,h and
1.10\,h \emph{net} --- so roughly half of what looks like an electric access
penalty is simply the cost of stopping, which any vehicle under the same break
regime pays. The genuinely electric access overhead nonetheless consumes about
half the coupling credit. This qualifies, rather than contradicts, the finding of
\citet{brockmann_break-and-charge_2025} that mandatory breaks create natural
charging windows: the windows exist and the model exploits them, but the access
overheads of a fast-charging stop, which that work does not price, absorb a
substantial part of the gain.

\medskip
The greedy comparison isolates the remaining half of the story. Greedy's electric
schedule is 19.9\% longer than its own diesel schedule on the short routes, 22.5\%
on the medium and 21.0\% on the long ones, so the penalty is flat in route length
once the corridor is long enough to be rest-dominated --- and about two and a half
times the optimal penalty. The excess is not electrification but planning
quality: measured against its own hindsight optimum, Greedy loses \prov{13.2\%}
(short) and \prov{14.4\%} (medium) on the electric problem but only \prov{2.1\%}
and \prov{1.6\%} on the diesel problem, where the myopic rule is nearly optimal
because the sole remaining decision is when to rest. Electrification is thus what
turns a scheduling problem that a rule of thumb solves almost optimally into one
where a rule of thumb costs a further 11--13 percentage points of makespan. Of
the 20--23\% an operator would observe when switching a myopically-scheduled
corridor from diesel to battery-electric, about half is the physical cost of
charging and about half would be recovered by adopting a planner such as those
compared in Section~\ref{sec:basecase}.''
\end{anew}

\input{additional_diesel_decomp}

\input{additional_diesel_refuel}

\begin{acmt}
Included because the caption of Table~\ref{tab:diesel-decomp} cross-references it;
without it the compiled paper reads ``Table~??''. It is generated by \texttt{additional\_figures.py}
and is also the table that justifies the one- and two-stop refuelling assumptions.
\end{acmt}

\begin{acmt}
The long-route column of Table~\ref{tab:diesel} and
Table~\ref{tab:diesel-decomp} carries an EV oracle certified only to
\prov{9.3\%} (against 0.5\% on the other two classes), so the $+10.8\%$ long-route
penalty is an upper bound on the true optimal one rather than a point estimate.
The ``EV cert.'' column already says this; make sure the prose does too, since
it is the one diesel number that is not solid.
\end{acmt}
